// Day Trader LIVE v0.14.1 - validation dashboard + NH safe scanner.
const $=s=>document.querySelector(s);const won=n=>'₩'+Math.round(Number(n||0)).toLocaleString('ko-KR');const pct=n=>(Number(n||0)>=0?'+':'')+Number(n||0).toFixed(2)+'%';let selectedCode='005930',lastData=null,updating=false,scanning=false,uiVersion='0.14.0';
function log(msg){const box=$('#logBox');if(!box)return;const d=document.createElement('div');d.className='logline';d.innerHTML=`<span>${new Date().toLocaleTimeString('ko-KR',{hour12:false})}</span> ${msg}`;box.prepend(d);while(box.children.length>60)box.lastChild.remove()}
function actionClass(a){return a==='BUY_CANDIDATE'?'up':a==='SETUP'||a==='SHADOW_ONLY'?'warn':a==='BLOCKED'||a==='SAFETY_WAIT'?'down':'neutral'}function nameOf(code){const s=lastData?.scanner?.find(x=>x.code===code),q=lastData?.quotes?.find(x=>x.code===code);return s?.name||q?.name||code}function labelOf(code){const n=nameOf(code);return n===code?code:`${n} ${code}`}function firstBlock(e){return(e?.blockedReasons||[])[0]||''}
function decisionText(e){if(e.action==='SAFETY_WAIT')return'안전 마스터 검증 대기';if(e.action==='BLOCKED')return firstBlock(e)||'안전필터 제외';if(e.action==='WAIT_DATA')return`5분봉 준비 ${e.indicators?.bars||0}/${e.indicators?.need||35}`;const c=e.indicators?.checks||{},m=e.market||{},miss=[];if(!c.session_ready)miss.push('개장30분 미완료');if(!m.liquidityOk)miss.push('유동성 기준 미달');if(!c.above_vwap)miss.push('VWAP 아래');if(!c.ema_bull)miss.push('EMA9≤20');if(!c.trigger_gate)miss.push('ORB30/강한돌파 없음');if(!c.rsi_ok)miss.push('RSI 범위 밖');if(!c.dmi_ok)miss.push('ADX/DMI 약함');if((e.marketBreadth??1)<.35&&e.score<90)miss.push('시장 breadth 약함');return miss.length?'보류: '+miss.slice(0,3).join(' · '):'진입 조건 충족'}
async function api(path,opt){const r=await fetch(path,{cache:'no-store',...opt});let j={};try{j=await r.json()}catch{}if(!r.ok)throw new Error(j.detail||`HTTP ${r.status}`);return j}function setBrand(){const b=document.querySelector('.brand');if(b)b.innerHTML=`Stock Day Trader <span>v${uiVersion} LIVE</span>`;const f=document.querySelector('footer');if(f)f.textContent=`Day Trader v${uiVersion} LIVE · Lenovo/NH · VALIDATION · REAL ORDER OFF`}function addCollapse(p,l){if(!p)return;const b=document.createElement('button');b.className='btn mobile-collapse-btn';b.textContent='펼쳐보기 · '+l;b.onclick=()=>{p.classList.toggle('mobile-collapsed');b.textContent=(p.classList.contains('mobile-collapsed')?'펼쳐보기 · ':'접기 · ')+l};p.appendChild(b);p.classList.add('mobile-collapsed')}
async function bootstrapUi(){try{const s=await api('/api/system/update/status');if(s.uiVersion)uiVersion=s.uiVersion}catch{}setBrand();const sub=document.querySelector('.sub');if(sub)sub.textContent='NH 실시간 · 안전필터 · 광역 스캔 · 활성40 · Top10 · v0.14 검증';const p=$('.connection-panel .panel-head');if(p&&!$('#remoteUpdateBtn')){const b=document.createElement('button');b.id='remoteUpdateBtn';b.className='btn secondary-action';b.textContent='↻ 업데이트';p.appendChild(b);b.onclick=runUpdate}if($('#backendUrl')){$('#backendUrl').value=location.origin;$('#backendUrl').readOnly=true}if($('#saveBackendBtn'))$('#saveBackendBtn').style.display='none';if($('#nhCheckBtn')){$('#nhCheckBtn').onclick=checkConnection;$('#nhCheckBtn').classList.add('utility-action');$('#nhCheckBtn').textContent='✓ 연결 확인'}if($('#scanBtn')){$('#scanBtn').onclick=runScan;$('#scanBtn').classList.add('scan-action');$('#scanBtn').textContent='↻ 재스캔'}if($('#backtestBtn')){$('#backtestBtn').disabled=true;$('#backtestBtn').textContent='실데이터 차트'}if($('#autoToggle')){$('#autoToggle').disabled=true;$('#autoToggle').checked=true}if($('#autoStateText'))$('#autoStateText').textContent='SERVER PAPER';if($('#killBtn')){$('#killBtn').disabled=true;$('#killBtn').textContent='REAL ORDER OFF'}if($('#resetBtn')){$('#resetBtn').disabled=true;$('#resetBtn').textContent='서버 데이터 보호'}if($('#maxOrderText'))$('#maxOrderText').textContent='비용 포함 Risk sizing';if($('#riskTradeText'))$('#riskTradeText').textContent='0.35%';if($('#maxPositionsText'))$('#maxPositionsText').textContent='2개';if($('#exitText'))$('#exitText').textContent='-1.0% · +0.8% 보호 · +1.5% Trail';if(matchMedia('(max-width:520px)').matches){const lows=document.querySelectorAll('.lower-grid');if(lows[1]){addCollapse(lows[1].children[0],'실패 원인 분석');addCollapse(lows[1].children[1],'시스템 로그')}addCollapse(document.querySelector('#tradesBody')?.closest('.panel'),'Paper 주문 내역')}}
async function checkConnection(){try{const h=await api('/api/health'),safe=h.universe?.verified,open=!!h.collector?.marketSession;$('#nhBadge').textContent=h.credentialsConfigured?'NH CONNECTED':'NH SERVER ONLY';$('#nhBadge').className='badge '+(h.credentialsConfigured?'ok':'');$('#brokerBadge').textContent=safe?'SAFE MASTER ON':'MASTER CHECK';$('#brokerBadge').className='badge '+(safe?'ok':'');$('#nhStatusText').innerHTML=`<b>Lenovo 정상</b> · Engine v${h.version} · ${h.mode} · 안전마스터 ${safe?'검증완료':'확인중'} · ${open?'KRX 장중':'KRX 장마감'} · v0.14 Validation · REAL ORDER OFF`;log(`연결 정상 · Engine ${h.version} · Validation ON`)}catch(e){$('#nhBadge').textContent='BACKEND OFF';$('#nhStatusText').textContent='연결 실패: '+e.message;log('연결 실패 · '+e.message)}}
function renderValidation(v){const box=$('#validationContent');if(!box)return;v=v||{};const pf=Number(v.profitFactor||0),ex=Number(v.expectancyPct||0),fails=Object.entries(v.failureTypes||{}).sort((a,b)=>b[1]-a[1]);box.innerHTML=`<div class="validation-grid"><div><small>표본</small><strong>${v.trades||0}건</strong></div><div><small>승률</small><strong>${Number(v.winRate||0).toFixed(1)}%</strong></div><div><small>Profit Factor</small><strong>${pf===999?'∞':pf.toFixed(2)}</strong></div><div><small>기대값/거래</small><strong class="${ex>=0?'up':'down'}">${pct(ex)}</strong></div><div><small>MDD</small><strong>${Number(v.maxDrawdownPct||0).toFixed(2)}%</strong></div><div><small>MFE / MAE</small><strong>${Number(v.avgMfePct||0).toFixed(2)} / ${Number(v.avgMaePct||0).toFixed(2)}%</strong></div></div><div class="score-buckets">${(v.scoreBuckets||[]).map(x=>`<div><small>Score ${x.bucket}</small><b>${x.trades}건 · 승률 ${x.winRate}%</b><br><small>평균 ${pct(x.avgPnlPct)}</small></div>`).join('')}</div><div class="failure-tags">${fails.length?fails.map(([k,n])=>`<span>${k} ${n}</span>`).join(''):'<span>손실 원인 표본 대기</span>'}</div><div class="validation-note">Control ${v.controlStrategy||'v0.8.0'} 고정 · 전략 자동변경 OFF · 순손익 ${won(v.netPnl||0)}</div>`}
function render(d){lastData=d;const safe=!!d.collector?.safetyVerified,marketOpen=!!d.collector?.marketSession,u=d.universe||d.collector?.universe||{};$('#systemBadge').textContent=d.collector.running?'SYSTEM RUNNING':'SYSTEM OFF';$('#systemBadge').className='badge '+(d.collector.running?'ok':'badbadge');$('#brokerBadge').textContent=safe?'SAFE FILTER ON':'SAFE CHECK';$('#brokerBadge').className='badge '+(safe?'ok':'');$('#nhBadge').textContent=marketOpen?'NH LIVE DATA':'NH CLOSE SNAPSHOT';$('#nhBadge').className='badge '+(marketOpen?'ok':'');if(d.daily.locked){$('#lockBadge').textContent='DAILY LOCK';$('#lockBadge').className='badge badbadge'}else if(!marketOpen){$('#lockBadge').textContent='MARKET CLOSED';$('#lockBadge').className='badge'}else{$('#lockBadge').textContent='TRADE READY';$('#lockBadge').className='badge ok'}const op=d.positions.reduce((s,x)=>s+Number(x.unrealized_pnl||0),0),capital=10000000+Number(d.daily.pnl||0)+op;$('#equity').textContent=won(capital);$('#equityPnl').textContent=`실현 ${won(d.daily.pnl)} · 평가 ${won(op)}`;$('#cash').textContent='서버 관리';$('#vault').textContent='통합 예정';$('#reserve').textContent='통합 예정';$('#lossStreak').textContent=`${d.daily.consecutiveLosses} / 2`;$('#shadowCount').textContent=d.paperLoop.shadowSignals||0;$('#shadowWin').textContent='Server Shadow';const all=[...(d.scanner||[])],ss=all.slice(0,10);const desc=document.querySelector('.main-grid article .panel-head p');if(desc)desc.textContent=`공식 마스터 안전후보 ${u.selectedRows||d.collector.universeSize||0} · 활성 ${all.length}/40 · Top10 · ORB30/VWAP/EMA/거래량`;$('#scannerBody').innerHTML=ss.map((s,i)=>{const x=s.indicators||{},m=s.market||{},activity=m.activityScore!=null?`활성 ${Number(m.activityScore).toFixed(0)} · ${Number(m.turnoverEok||0).toFixed(0)}억`:'';return `<tr class="pick-row" data-code="${s.code}"><td>${i+1}</td><td><b>${s.name||nameOf(s.code)}</b><br><small>${s.code}</small></td><td>${x.price?won(x.price):'-'}</td><td>${m.changeRate!=null?pct(m.changeRate):'-'}</td><td><span class="score">${Number(s.score||0).toFixed(1)}</span><br><small>${decisionText(s)}</small><br><small class="neutral">${activity}</small></td><td>${x.vwap?won(x.vwap):'-'}</td><td>${x.volumeRatio?Number(x.volumeRatio).toFixed(2)+'x':'-'}</td><td class="${actionClass(s.action)}">${s.action}</td><td>서버 자동</td></tr>`}).join('')||'<tr><td colspan="9" class="neutral">활성 후보 준비 중</td></tr>';document.querySelectorAll('.pick-row').forEach(r=>r.onclick=()=>{selectedCode=r.dataset.code;renderSelected()});$('#positionsBody').innerHTML=d.positions.length?d.positions.map(p=>`<tr><td><b>${nameOf(p.code)}</b><br><span class="neutral">${p.code} · SERVER PAPER</span></td><td>${p.qty}</td><td>${won(p.entry_price)}</td><td>${won(p.current_price)}</td><td class="${p.unrealized_pnl>=0?'up':'down'}">${won(p.unrealized_pnl)}</td><td>${pct(p.unrealized_pct)}</td><td>자동관리</td></tr>`).join(''):'<tr><td colspan="7" class="neutral">포지션 없음</td></tr>';$('#tradesBody').innerHTML=d.recentTrades?.length?d.recentTrades.slice(0,30).map(t=>`<tr><td>${t.exit_at?new Date(t.exit_at).toLocaleTimeString('ko-KR',{hour12:false}):'-'}</td><td>${labelOf(t.code)}</td><td>CLOSE</td><td>${t.qty}</td><td>${t.exit_price?won(t.exit_price):'-'}</td><td class="${Number(t.pnl||0)>=0?'up':'down'}">${won(t.pnl)}</td><td>${t.exit_reason||t.status}${t.failure_type&&t.failure_type!=='WIN'?'<br><small>'+t.failure_type+'</small>':''}</td></tr>`).join(''):'<tr><td colspan="7" class="neutral">거래 없음</td></tr>';let rt='NORMAL · 안전필터 ON · 비용반영 Risk',rc='okbox';if(d.daily.locked){rt='DAILY LOCK · 신규진입 중지 · Shadow 계속';rc='badbox'}else if(!safe){rt='SAFETY WAIT · 신규진입 차단';rc='badbox'}else if(!marketOpen)rt='MARKET CLOSED · 시세 스냅샷/분석만 유지';$('#riskState').textContent=rt;$('#riskState').className='risk-state '+rc;$('#strategyStats').innerHTML=`<div><small>Safe Universe</small><strong>${u.selectedRows||0}</strong></div><div><small>Active Focus</small><strong>${all.length}/40</strong></div><div><small>Entry Window</small><strong>${d.entryStart||'09:30'}~${d.entryCutoff}</strong></div><div><small>EOD Exit</small><strong>${d.eodExit}</strong></div><div><small>Cost Est.</small><strong>${Number(d.risk?.roundTripCostEstimatePct||0).toFixed(2)}%</strong></div><div><small>Max Positions</small><strong>${d.risk?.maxOpenPositions||2}</strong></div>`;$('#learningBox').innerHTML=`<div class="learning-stats"><b>Shadow ${d.paperLoop.shadowSignals||0}</b><b>종료 ${d.daily.closedTrades||0}</b><b>연속손실 ${d.daily.consecutiveLosses||0}/2</b></div><p>v0.14는 진입 스냅샷, MFE/MAE, Score 구간, 실패원인을 기록합니다. Control 전략은 자동 변경하지 않습니다.</p>`;renderValidation(d.validation);if((!selectedCode||!all.some(x=>x.code===selectedCode))&&ss.length)selectedCode=ss[0].code;renderSelected()}
async function renderSelected(){if(!lastData)return;const s=(lastData.scanner||[]).find(x=>x.code===selectedCode)||(lastData.scanner||[])[0];if(!s)return;selectedCode=s.code;const i=s.indicators||{},m=s.market||{};$('#chartTitle').textContent=`${s.name||labelOf(s.code)} ${s.code} · NH 5분봉`;$('#indicatorCards').innerHTML=`<div><small>판정</small><strong>${s.action}</strong></div><div><small>Score</small><strong>${Number(s.score||0).toFixed(1)}</strong></div><div><small>VWAP</small><strong>${i.vwap?won(i.vwap):'-'}</strong></div><div><small>EMA9/20</small><strong>${i.ema9&&i.ema20?Math.round(i.ema9).toLocaleString()+' / '+Math.round(i.ema20).toLocaleString():'-'}</strong></div><div><small>RSI</small><strong>${i.rsi!=null?Number(i.rsi).toFixed(1):'-'}</strong></div><div><small>ADX</small><strong>${i.adx!=null?Number(i.adx).toFixed(1):'-'}</strong></div>`;$('#backtestResult').textContent=`${decisionText(s)} · ORB30 ${i.openingRangeHigh?won(i.openingRangeHigh):'-'} · 거래대금 ${Number(m.turnoverEok||0).toFixed(1)}억 · 시장 breadth ${(Number(s.marketBreadth||0)*100).toFixed(0)}% · 완료봉 기준`;try{const j=await api(`/api/market/bars/${s.code}?limit=80&u=${Date.now()}`);drawChart(j.rows||[],i.vwap)}catch(e){log('차트 오류 · '+e.message)}}
function ema(v,p){if(!v.length)return[];const a=2/(p+1),o=[];let x=Number(v[0]);o.push(x);for(let i=1;i<v.length;i++){x=a*Number(v[i])+(1-a)*x;o.push(x)}return o}function drawChart(rows,vwap){const c=$('#priceChart');if(!c)return;const ctx=c.getContext('2d'),r=devicePixelRatio||1,w=c.clientWidth||600,h=c.clientHeight||300;c.width=w*r;c.height=h*r;ctx.setTransform(r,0,0,r,0,0);ctx.clearRect(0,0,w,h);rows=(rows||[]).filter(x=>Number(x.open)>0).sort((a,b)=>String(a.bucket).localeCompare(String(b.bucket)));if(rows.length<2){ctx.fillStyle='#94a3b8';ctx.fillText('5분봉 데이터 준비 중',12,22);return}const hi=Math.max(...rows.map(x=>Number(x.high))),lo=Math.min(...rows.map(x=>Number(x.low))),pad=(hi-lo||1)*.08,H=h-28,ys=p=>H-(p-(lo-pad))/((hi+pad)-(lo-pad))*(H-8)+4,step=(w-16)/rows.length,bw=Math.max(2,Math.min(7,step*.58));rows.forEach((b,i)=>{const x=8+(i+.5)*step,o=Number(b.open),cl=Number(b.close);ctx.strokeStyle=cl>=o?'#4ade80':'#fb7185';ctx.fillStyle=ctx.strokeStyle;ctx.beginPath();ctx.moveTo(x,ys(Number(b.high)));ctx.lineTo(x,ys(Number(b.low)));ctx.stroke();ctx.fillRect(x-bw/2,Math.min(ys(o),ys(cl)),bw,Math.max(1,Math.abs(ys(o)-ys(cl))))});const closes=rows.map(x=>Number(x.close));[[ema(closes,9),'#60a5fa'],[ema(closes,20),'#f59e0b']].forEach(([a,col])=>{ctx.strokeStyle=col;ctx.beginPath();a.forEach((p,i)=>{const x=8+(i+.5)*step;i?ctx.lineTo(x,ys(p)):ctx.moveTo(x,ys(p))});ctx.stroke()});ctx.fillStyle='#94a3b8';ctx.fillText('5분봉 · EMA9/20 · VWAP',8,h-8)}
async function load(){try{render(await api('/api/mobile/status?u='+Date.now()))}catch(e){$('#systemBadge').textContent='SERVER OFF';$('#systemBadge').className='badge badbadge';log('갱신 실패 · '+e.message)}}async function runScan(){if(scanning)return;scanning=true;const b=$('#scanBtn');if(b){b.disabled=true;b.textContent='⏳ 스캔'}try{const j=await api('/api/paper/scan?u='+Date.now());await load();log(`광역 재스캔 · 활성 ${(j.rows||[]).length}`)}catch(e){log('스캔 실패 · '+e.message)}finally{setTimeout(()=>{scanning=false;if(b){b.disabled=false;b.textContent='↻ 재스캔'}},900)}}async function runUpdate(){if(updating)return;if(lastData?.positions?.length){alert('열린 Paper 포지션이 있어 업데이트할 수 없습니다.');return}if(!confirm('최신 버전을 확인하고 필요한 경우 업데이트할까요?'))return;updating=true;const b=$('#remoteUpdateBtn');if(b){b.disabled=true;b.textContent='확인 중...'}try{await api('/api/system/update/run',{method:'POST'})}catch(e){alert('업데이트 차단: '+e.message);updating=false;if(b){b.disabled=false;b.textContent='↻ 업데이트'}return}let down=false,n=0;const t=setInterval(async()=>{n++;try{const r=await fetch('/api/health?u='+Date.now(),{cache:'no-store'});if(!r.ok)throw 0;if(down){clearInterval(t);location.replace('/classic?u='+Date.now())}}catch{down=true}if(n>90){clearInterval(t);updating=false;alert('서버 재연결 시간이 초과되었습니다.')}},1000)}bootstrapUi().then(()=>{checkConnection();load();setInterval(()=>{if(!updating&&!scanning)load()},5000)});addEventListener('resize',()=>renderSelected());log('Day Trader v0.14.1 LIVE · VALIDATION · REAL ORDER OFF');